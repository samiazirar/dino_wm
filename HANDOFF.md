# CURRENT HANDOFF 2026-07-21: STOPPED FOR OWNER REVIEW

The human owner is final authority. **Current state is `STOPPED_FOR_OWNER_REVIEW`.** Task 57 is documentation synchronization only; no repair, architecture, implementation, probe, launch, scheduler, watcher, or paper worker is active. Do not resume from an earlier READY proposal. Read the complete evidence chain `reports/39_pusht_empirical_proxy_amendment.md` through `reports/65_independent_final_empirical_repair_audit.md`, with the capsule subchain `41`-`59` and empirical implementation/audit subchain `40`, `45`, `47`, `52`, `56`, `60`-`65`.

`reports/39_pusht_empirical_proxy_amendment.md` authorizes only the PushT empirical proxy/intervention amendment and bounded implementation/audit: `dinocular` uses `float32(wire_f16) * 1.5746406149864196`; `dinocular_zerodepth` uses exact constant-zero numeric intervention; `[ASSUMPTION: RECOVERED-CONTRACT]` and the negative/non-equivalence claims remain binding; Wall, Rope, and Granular remain unchanged. `reports/43_execution_threat_model_amendment.md` narrows the execution threat model and selects a monolithic read-only capsule direction, but grants no implementation, capsule construction, target probe, scheduler, or GPU authority.

The independently reviewed capsule sequence is terminal. Successive proposals in reports 41, 44, 48, 50, 54, and 58 were independently rejected in reports 42, 46, 49, 51, 55, and finally 59. Report 59 (`efe7c031001c38ad77baced94200b6e641e79b47053181d24459c54bc3816af0`) records `INDEPENDENT_CAPSULE_ARCHITECTURE_ERRATA_REJECT` with four unclosed contradictions. Under reports 57-59, capsule work is `STOPPED_FOR_OWNER_REVIEW`; there is no implementation-planning, engineering, construction, or CPU/no-GPU probe authority.

The empirical candidate sequence is also terminal. Report 64 is **BLOCKED** (`ac9356b8bf1707202ddc88315b618cf20ab72f05632d70d99138defbe304be97`). Report 65 (`507036b8791fd7508e9d6703bee5c4e8d6e57be57d6c512c248eb0bfb79182c8`) passes only the truthfulness of that stop record; it is not implementation acceptance. The candidate remains rejected and non-executable, and empirical implementation is `STOPPED_FOR_OWNER_REVIEW` under reports 63-65.

Exactly two empirical defects remain open: (1) lexical pre-open alias/symlink rejection is incomplete at remaining host-evidence sites, including chain manifest/progress/checkpoint and evaluation completion-reference paths; and (2) no single loader-produced accepted-evidence test covers the full materialize -> live/submit -> chain -> reader -> encoder -> receipt-classification route. The adjacent suites remain exactly `15 failed, 102 passed, 1 skipped`; the direct empirical suite's `61 passed` does not override those defects.

All production prerequisites remain null/unavailable and fail closed: accepted native-v1 index; empirical runtime release; independent runtime-release acceptance; source release; empirical implementation acceptance; immutable execution acceptance; immutable probe acceptance; explicit hash-bound launch authorization; deployment acceptance; capsule record. `execution_authority_granted=false`. No candidate hash or synthetic fixture substitutes for an accepted artifact or authority.

Accepted `dino_wm` HEAD remains `158c74704bd18343dfbccb2fb25f4b6726c33f93`. The rejected candidate is dirty and uncommitted: 17 tracked modified files, 2,415 insertions and 205 deletions, plus six relevant untracked candidate files (two empirical encoder configs, two files under `contracts/`, `empirical_depth_contract.py`, and `tests/test_pusht_empirical_contract.py`). Unrelated `.opencode/` remains outside scope. Preserve this candidate for owner review; do not commit, deploy, materialize, or execute it.

Accepted cache state is unchanged and unconsumed. Rope and Granular quarantine caches retain independent `CACHE_PASS` and remain unpromoted/unconsumed. Accepted PushT MapAnything remains manifest `b87fe658-4731-4e8e-8c88-38f4fac6344c`, `data.mdb` SHA-256 `68fe99e9f566eadcaa61e092b042962060fdc82c39ee8e71dcd7b9ef05247adb`, and `[ASSUMPTION: RECOVERED-CONTRACT]`; it remains unconsumed. No cache mutation or promotion occurred.

The paper identity is frozen and untouched: PaperPilot project `6a5662553c2ec1b86de59f76`, revision `fork-34b3737794bdee49`, empty author diff fingerprint `f477b038b9cecd19721476e964d517ad44c055b4a0e20f084799218600df5db5`, and seven-page PDF `reports/ECCV_SUBMISSION_DRAFT.pdf` SHA-256 `0ad346391a67532421548868c63b08a7f8564666c19bf9531ab23e152a1d4af5`. It is owner-upload-ready and not externally submitted.

There is no Dinocular scheduler job, watcher, GPU allocation/use, training, evaluation, cache mutation/consumption, commit, deployment, capsule construction, target probe, result promotion, or paper edit. No launch or concurrency expansion is authorized. All older task descriptions below remain provenance only; any stale pane, worker, repair, architecture, watcher, or restart language is retired where it conflicts with this overlay.

# RESOLVED 2026-07-17: Molmo1M is OUT OF SCOPE (boss struck it)

The boss (Sami) explicitly disavowed the "use the Molmo1M dataset" line on 2026-07-17. It makes no sense for this campaign and was never a real requirement. "Molmo1M" entered the project through a single uncorroborated line in `prompts/27_ceo_replan.md`, which asserted a boss order while noting that no reference to Molmo exists anywhere else in the repo (no script, config, dataset, or matrix cell). No Molmo work was ever done. It is permanently out of scope. Do not download, stage, inspect, train on, or report Molmo1M unless the boss re-opens it in writing. This is no longer an open question.

# DINOcular x DINO-WM CEO handoff

Updated: 2026-07-17T17:23:06+02:00  
Standing CEO pane at handoff: `w3:p19`. Codex weekly budget is now 6 percent. Claude Opus panes are the primary workhorse for operations, reading, judgment, paper work, and verification. The remaining Codex budget is reserved only for critical `gpt-5.6-sol` medium coding such as a necessary Slurm script or wrapper fix. No Codex model performs monitoring, reading, routine gate work, or paper work. Every task has a hard cap below.

This file is the single takeover source of truth. `STATUS.md` and `STATUS_PAPER.md` are append-only evidence logs. When live facts change, update this file's current-state lines and the appropriate status file in the same worker turn.

## 1. Mission and hard dates

The mission is to deliver the strongest honest, reproducible DINOcular x DINO-WM ECCV workshop submission that the accepted evidence permits.

- Final rebuilt and cross-reviewed PDF: `reports/ECCV_SUBMISSION_DRAFT.pdf` by 2026-07-19 evening.
- Boss manual submission: 2026-07-20 regardless of result completeness.
- From 2026-07-18 12:00 Europe/Berlin, folding accepted numbers, validating the paper, and rebuilding the PDF outrank starting new runs.
- The 36-cell matrix, three arms, three paired seeds, two-cell PushT P2a producer pilot, Marvin as primary compute, and submission date are locked.
- Unmeasured outcomes stay TBD. No result enters the paper until its acceptance evidence is explicit.

## 2. Open questions for the boss

### OPEN QUESTION #1: Molmo1M scope [RESOLVED 2026-07-17 - OUT OF SCOPE]

Struck by the boss on 2026-07-17. Molmo1M is out of scope permanently (see the top of this file). No Molmo work, ever, unless the boss re-opens it in writing.

### OPEN QUESTION #2: Pending paper proposal

`reports/paper_pending_approval_20260717.md` contains R1 to R6. The paper checkpoint recommends approving R1 to R4 subject to a seven-page rebuild, rejecting R5.1 so the abstract keeps its honest no-outcomes statement, and approving R5.4 to R5.7 as redundant marker removal. The boss must record a red/green verdict for every proposed prose hunk before any nonmechanical source edit. If no verdict arrives, ship the already submission-ready PDF with 79 honest TBD markers.

### OPEN QUESTION #3: P2a launch preference

No launch decision is needed until both PushT caches are independently VALID. At that event the CEO requests a one-line boss verdict. If the boss is unreachable and the final cache gates pass, this handoff authorizes the standing CEO to record the explicit GO and launch exactly the locked two-cell pilot. No broader training launch is implied.

## 3. Current accepted and live state

**GOVERNING PUSHT COMPARISON CLOSURE (supersedes every older C3/C5 restart instruction below):** the one permitted fresh-calibration submission deadline was `2026-07-18 12:00 CEST` and is missed. PushT Depth Anything 3 is stopped with no retry or deadline slide. MapAnything is independently VALID; Depth Anything 3 is unavailable by the cutoff; therefore no fair pilot comparison exists and pilot/P2a training is unauthorized. Both DA3 graphs are terminal with no live owned jobs. Preserve `shard-000-of-008` and `shard-001-of-008` unchanged as evidence-only empty `.da3-work/` stubs requiring quarantine. The sole stale repository watcher, PID `3375798` targeting retired pane `w3:pH`, was cleanly stopped with `TERM`; no owner watcher remains and none should be started for this graph. Evidence: `reports/26_da3_stop_closure.md`.

Bounded Marvin accounting at 2026-07-17T17:23:06+02:00:

- Rope: failed root `26547764` was fixed at commit `27b73d7e...110a`; the one authorized replacement `26555357` completed and is independently VALID. This task is closed. Preserve the rejected cache in quarantine.
- Granular: failed root `26547765` has the same evidenced deterministic producer fix. Its one authorized replacement `26555358` COMPLETED `0:0` (ended 2026-07-17T17:28:09 on `sgpu026`) and is independently **VALID** (accepted manifest `52897463-5cc8-4f70-b999-5f95af9d8b3f`, closed data `4b6b3238...c23349`, PASS receipt `granular.52897463-...json`) per the independent `granular-acceptance-audit` operator; every hash, count, gate value, deterministic contract, and preserved quarantine artifact was rechecked against ground truth and CONFIRMED. C1 is closed. Preserve the rejected cache in quarantine. Terminal `26555358` retired from the watch list. Evidence: `reports/23_granular_acceptance.md`.
- PushT DA3: calibration and deterministic eight-shard plan job `26548668` completed `0:0` but is purged from the live controller, so an `afterok:26548668` edge is impossible; the stale edge was omitted and the prerequisite enforced by the shard sbatch plan.json SHA-256 check (`3cd6d52d...fea2fa`). The eight shards `26557616`-`26557623` were submitted (report 22, audit VALID) but ALL then `FAILED 2:0` in 33-45 s and merge `26557625` never started. **Diagnosis (2026-07-18, report 25): localized non-scientific wrapper defect** — the DA3 PushT GPU-producer sbatch wrappers never pass `--env CUBLAS_WORKSPACE_CONFIG=:4096:8`, so the shared producer's determinism fail-close (`configure_deterministic_producer` in `precompute_depth.py`) aborts at producer `__init__` with `ContractError` -> `SystemExit(2)`. (Calibrate-plan `26548668` predated the determinism fix, hence it passed; plan/calibration remain immutable and intact.) Smallest fix applied **local repo only** (NOT deployed, NOT resubmitted): added the one `--env CUBLAS_WORKSPACE_CONFIG=:4096:8` line to `da3_pusht_shard.sbatch` (-> sha256 `e363e4c3...6764ae`) and `da3_pusht_merge_validate.sbatch` (-> `00979324...4107de`, which had the identical latent defect at its step-2 recompute); `da3_pusht_calibrate_plan.sbatch` left unchanged (done/immutable). Focused regression added to `test_depth_cache.py` (`16 passed`). No shard root or `pusht.lmdb` exists; the one authorized C3 replacement was NOT consumed (wrapper defect => corrected re-run is the same science, not a producer substitution or reshard). **Corrected rerun executed 2026-07-18** (after main independent review PASS): cancelled only the dead merge `26557625`; deployed only the two fixed wrappers to Marvin `code/dino_wm/tools/` (remote sha256 == local, shard `e363e4c3...6764ae`, merge `00979324...4107de`); resubmitted exactly eight shards indices 0..7 = `26570272`(0) `26570273`(1) `26570274`(2) `26570275`(3) `26570276`(4) `26570277`(5) `26570278`(6) `26570279`(7) with `Dependency=(null)` + plan-hash fail-close, and merge-validator `26570280` with live `afterok:` on all eight new IDs; `scontrol` re-read confirmed every ID/resource/export/dependency and shard `26570272` was already RUNNING on `sgpu028`. Watcher atomically swapped to the nine new IDs (old dead IDs removed, `.previous` rebaselined). Failed `26557616`-`26557623` roots/logs preserved. **C3 HARD-BLOCKED 2026-07-18** — the wrapper fix worked (producer now initializes), but that unmasked a second, deeper contract failure: replacement shards `26570272`/`26570273` `FAILED 2:0` with `calibration manifest producer differs from the pinned current producer`. Root: the PushT calibration `pusht.json` (`b7b8564b...ee2fd`) and the plan embedding it (`3cd6d52d...fea2fa`) were minted by `26548668` at ~01:28 Jul 17, BEFORE the determinism fix (~14:48); their producer provenance lacks `settings.deterministic_execution`, while the pinned fixed producer's provenance includes it, so `producer_identity()` (which retains that block) rejects the mismatch at `precompute_depth.py:1427`. This is a producer/calibration/plan provenance incompatibility, NOT a wrapper defect, and is OUTSIDE the "no calibration/plan/producer change" mandate. Main cancelled shards `26570274`-`26570279` + merge `26570280`; `26570272`/`26570273` `FAILED 2:0` (logs preserved). Two non-clean partial roots `shard-000-of-008` / `shard-001-of-008` (empty `.da3-work` only, no depth data) were created and must be removed/quarantined before any future index-0/1 rerun. Immutable `plan.json`/`pusht.json` unchanged; no `pusht.lmdb`; fixed wrappers remain deployed (correct, not the blocker). **A boss-visible decision is required before any recalibrating/replanning**: either (a) authorize regenerating the PushT calibration + plan with the pinned fixed producer (new immutable hashes, updated C3 contract) then rerun 8 shards + merge, or (b) authorize a contract amendment to the producer-identity comparison. No recalibration, replan, producer change, or resubmission was performed. Evidence: `reports/25_pusht_da3_shard_diagnosis.md` (`## HARD BLOCK`).
- MapAnything: **FULL CACHE ACCEPTED / VALID (C4, 2026-07-18)**. Replacement shards `26547709` to `26547712` completed `0:0`; failed merge `26547714` was root-fixed in wrapper SHA-256 `151cb10e...e2e`. The one replacement merge-validator `26556560` **COMPLETED 0:0** (`02:07:26`, `sgpu029`, ended `2026-07-17T18:40:12`). Accepted full-cache manifest `b87fe658-4731-4e8e-8c88-38f4fac6344c`, PASS receipt `depth_cache_mapanything_singleton/validation.json` (`state=PASS`), merged `data.mdb` SHA-256 `68fe99e9...5247adb` independently re-hashed == manifest; 18,706 traj / 2,339,250 frames, all eight payloads, `source_index ed6eecd6...`, `tool 40ea2187...`, `shard_plan 4569ae4e...23f`, calibration `pusht_training_only` all verified. Frame-wise temporal is characterization-only (non-blocking). `[ASSUMPTION: RECOVERED-CONTRACT]` applies. Terminal `26556560` retired from the watch list. This is one of the two PushT prerequisites; DA3 PushT (C3) is not yet accepted. **Independently CONFIRMED VALID (2026-07-18)** by the fresh independent acceptance auditor: terminal `sacct` accounting, a full 102 GB merged-`data.mdb` re-hash (`68fe99e9...05247adb` == manifest), the authoritative in-SIF LMDB entry count (`2,339,250` entries / `18,706` distinct episodes), an independent manifest recompute (18,706 records, 2,339,250 unique keys, eight disjoint shards union 18,706), identity/provenance, receipt linkage, preserved `.failed-26547714`, and no live merge duplicate were all rechecked from ground truth and matched; `MAPANYTHING_INDEPENDENT_VALID`. Evidence: `reports/24_mapanything_merge_acceptance.md` (`## INDEPENDENT AUDIT VERDICT`).
- P2a: not launched. Resume chaining is accepted at base commit `12374e4`; strict execution requires one process, `env.num_workers=0`, deterministic algorithms, TF32 disabled, and `CUBLAS_WORKSPACE_CONFIG=:4096:8`.
- Paper: current PDF is already seven pages and upload-ready, with 79 honest TBD markers and no accepted P2a, P3, P4, or P5 outcome cells. Pending prose proposal remains unapplied.

Current watcher set as of 2026-07-18 (C3 corrected rerun): PushT DA3 replacement shards `26570272`-`26570279` + PushT DA3 merge `26570280` (the failed `26557616`-`26557623` + cancelled merge `26557625` removed; terminals `26555358` and MapAnything merge `26556560` retired earlier; `26556560` accepted VALID, see section 3). The external `owner_event_watch.sh` daemon was restarted DETACHED (setsid, PID 1741618, survives pane closure) and retargeted from `w3:p1B` to `w3:p1C` after pane `w3:pW` was closed and killed the old pane-coupled daemon; campaign job events now wake the campaign owner directly. Any worker that creates or retires a live owned job must atomically update `scratch/owner_watch_jobs.txt` in the same turn.

## 4. Task decomposition through submission

Each task has one fresh, tight-context owner and a hard weighted-rollout-token cap. The cap is not a target. Checkpoint early and stop at the cap. Workers write evidence to disk before reporting completion. Thresholds change only through a boss-visible `MASTER_PLAN.md` amendment.

### C1. Granular replacement acceptance

- Owner: fresh Claude Opus pane `granular-acceptance` after takeover.
- Tier: primary Opus operations workhorse.
- Model: Claude Opus.
- Effort: bounded knight-tier verification.
- Token budget: 8,000 Opus tokens maximum; 0 Codex.
- Inputs: job `26555358`; Granular root-fix checkpoint in `STATUS.md`; failed and quarantined `26547765` evidence; unchanged validation tools and pinned SIF.
- Outputs: fresh immutable manifest, external receipt, acceptance report, `STATUS.md` update, and watcher-list update.
- Done criteria: job completes `0:0`; exact keys/counts, wire format, range, distribution, training-only calibration, provenance, unchanged `0.001` whole-trajectory gate, and DA3 temporal gates pass; rejected evidence remains unchanged.
- Verification: fresh independent `granular-acceptance-audit` operator rechecks hashes, source index, deterministic contract, recomputation maximum, and accounting before VALID.
- Failure handling: preserve artifacts and identify the exact failed gate. The authorized replacement is consumed. No second resubmission, threshold change, or producer substitution without amendment and boss decision.
- Restart without loss: read this task, the current ORCHESTRATOR block, the worker report, job `26555358`, and its receipt only. Resume at the first unchecked acceptance gate.

### C2. Rope closure preservation

- Owner: CEO evidence index. No active worker.
- Tier: closed record.
- Model: none.
- Effort: none.
- Token budget: 0.
- Inputs: jobs `26547764` and `26555357`; manifest `0a31897e...9fb3`; accepted receipt and quarantine record in `STATUS.md`.
- Outputs: stable accepted Rope reference for later eligibility.
- Done criteria: already met. The replacement completed, unchanged gates passed, and rejected evidence is quarantined.
- Verification: consumers match accepted identifiers and hashes without rerunning the gate.
- Failure handling: later hash drift invalidates consumption and starts bounded forensics. No automatic rebuild or fallback.
- Restart without loss: read only the Rope acceptance checkpoint in `STATUS.md` and verify its manifest and receipt identifiers.

### C3. PushT DA3 stale-dependency repair and production chain

- **Terminal override (2026-07-18): CLOSED / STOPPED, no retry.** The `12:00 CEST` fresh-calibration deadline is missed. Do not recalibrate, replan, resubmit, amend the producer contract, clean/reuse the two partial roots, or restart a watcher. Preserve all evidence; the two empty roots require quarantine.

- Owner: fresh Claude Opus pane `pusht-da3-production`; add one Codex Sol-medium worker only if a critical script or wrapper code fix is proved necessary.
- Tier: primary Opus operations workhorse with critical coding reserve.
- Model: Claude Opus primary; Codex `gpt-5.6-sol` only for the critical code patch.
- Effort: bounded Opus knight work; Codex medium only.
- Token budget: 16,000 Opus maximum plus 8,000 Codex maximum only if the code-fix gate opens.
- Inputs: completed `26548668`; calibration SHA-256 `b7b8564b...ee2fd`; plan-file SHA-256 `3cd6d52d...fea2fa`; plan-content SHA-256 `7fefd05d...248eac`; eight-shard contract in `STATUS.md`; current production and merge wrappers.
- Outputs: any necessary root-cause wrapper fix with focused tests; eight shard IDs; one merge-validator ID; exact submission report; atomic watcher update; `STATUS.md` entry.
- Done criteria: prove the parent completed, artifact hashes match, no duplicate exists, and Slurm purged the parent. Submit exactly shard indices `0..7` with unchanged science and direct merge dependencies. The impossible stale live edge may be omitted only with the completed prerequisite and artifact hashes recorded. Re-read every ID, resource, export, and dependency from Slurm.
- Verification: different fresh `pusht-da3-submission-audit` worker compares code diff, tests, submissions, immutable plan, dependency graph, duplicates, and watcher membership.
- Failure handling: terminal stop. Preserve all failed logs/artifacts and the two empty partial roots; no retry, producer substitution, threshold edit, dependency replacement, scientific reshard, calibration, or plan regeneration is authorized.
- Restart without loss: **do not restart C3.** Read `reports/26_da3_stop_closure.md` only to preserve the terminal record and quarantine requirement.

### C4. MapAnything repaired merge and full-cache acceptance

- Owner: fresh Claude Opus pane `mapanything-merge-gate` after takeover.
- Tier: primary Opus operations workhorse. Any new code fix becomes a separate Sol-medium task and requires boss authority because the repaired replacement is consumed.
- Model: Claude Opus; Codex `gpt-5.6-sol` medium only if a critical authorized code fix is required.
- Effort: bounded Opus knight work.
- Token budget: 10,000 Opus maximum; Codex remains 0 unless the boss opens a critical code-fix task with its own cap.
- Inputs: completed shards `26518202` to `26518205` and `26547709` to `26547712`; `26556560`; plan SHA-256 `4569ae4e...23f`; wrapper repair evidence in `STATUS.md`.
- Outputs: merged immutable manifest, PASS receipt, gate report, `STATUS.md` update, and retired watcher ID.
- Done criteria: exactly 18,706 trajectories and 2,339,250 frames; all eight payloads; assignment, common producer/calibration, source index, LMDB hashes, applicable hard gates, and receipt linkage pass. Frame-wise temporal values remain characterization only.
- Verification: fresh independent operator checks manifest, receipt, inventory, payload identities, and accounting before VALID.
- Failure handling: preserve output and logs, determine the exact cause, and do not launch another merge. Escalate any further run or amendment.
- Restart without loss: read this task, the MapAnything checkpoint, latest gate report, job `26556560`, and produced manifest/receipt only. Resume at the first unchecked gate.

### C5. PushT cache readiness and explicit P2a GO

- **Terminal override (2026-07-18): CLOSED / NOT ELIGIBLE.** MapAnything is VALID but DA3 is unavailable by the cutoff, so the two-cache prerequisite cannot be met and no fair pilot comparison or pilot training is authorized.

- Owner: fresh Claude Opus decision pane; boss or successor CEO records the final GO from its evidence.
- Tier: scientific launch decision.
- Model: Claude Opus, with the Fable advisor as boss check.
- Effort: bounded knight-tier decision and audit.
- Token budget: 12,000 Opus maximum; 0 Codex.
- Inputs: independently accepted DA3 and MapAnything full-cache receipts; DONE-23 resume evidence; recovered-contract assumption; compute and token budgets.
- Outputs: recorded GO or fail-closed reason, one boss request, and readiness sentinel only after both caches are VALID.
- Done criteria: both identities and every hard gate are accepted, no pilot duplicate exists, exact two-cell configs are materialized, and decision author is named.
- Verification: Opus attacks both cache records and the exact config diff.
- Failure handling: never launch from the single available MapAnything cache. C3 is terminally stopped; do not route this closure back into a restart path.
- Restart without loss: **do not restart C5 or seek a GO.** Preserve the no-comparison conclusion in `reports/26_da3_stop_closure.md`.

P2A GO: TERMINALLY NOT AUTHORIZED FOR THIS COMPARISON (DA3 unavailable by the cutoff; no fair pilot comparison).

### C6. Locked two-cell P2a execution

- Owner: fresh Claude Opus panes `p2a-launch-and-chain` and `p2a-result-audit`; add Codex only for a proved critical script fix.
- Tier: primary Opus production workhorse with critical coding reserve.
- Model: Claude Opus primary; Codex `gpt-5.6-sol` medium only for critical code.
- Effort: bounded Opus knight work; Codex medium only.
- Token budget: 20,000 Opus launch/chain plus 12,000 Opus audit maximum; at most 8,000 Codex only if a critical code-fix task opens.
- Inputs: GO; accepted caches; resume commit `12374e4`; exact seed-1 dinocular configs; 123,858-step target; fixed held-out manifest; recovered-contract tag.
- Outputs: exactly two cells, deterministic resume chains, checkpoints, curves, held-out pooled-NRE records, convergence evidence, accounting, and `STATUS.md` entries.
- Done criteria: one process, `env.num_workers=0`, deterministic algorithms, TF32 off, fixed cuBLAS contract, exact cursor and RNG resume, identical non-producer settings, target steps complete, finite losses, and valid horizon-5 and horizon-10 pooled-NRE denominators.
- Verification: result auditor checks config parity, continuity, target completion, hashes, convergence, and aggregation.
- Failure handling: stop at the first invalid checkpoint and fix the root cause. No reduced steps, changed seed, alternate metric, fallback cache, or post-result tuning.
- Restart without loss: read this task, GO, latest chain report, last accepted atomic checkpoint hash, remaining target steps, and live IDs. Resume only through the proven cursor-aware chain.

### C7. P2a adjudication

- Owner: fresh Claude Opus adjudication pane with Fable boss check.
- Tier: scientific judgment.
- Model: Claude Opus primary; Fable advisor for the boss check.
- Effort: bounded knight-tier judgment.
- Token budget: 16,000 Opus maximum; 0 Codex.
- Inputs: both independently verified P2a bundles.
- Outputs: producer amendment, accepted report, paper-eligible ledger, and recovered-contract disclosure.
- Done criteria: apply only the locked lower pooled-NRE point-estimate rule with valid horizon-5 and horizon-10 denominators and unchanged convergence criteria.
- Verification: Opus recomputes the decision and attacks leakage, aggregation, parity, and any post hoc rule.
- Failure handling: invalid or undefined comparison becomes inconclusive and escalates. Never invent a tiebreaker.
- Restart without loss: read both result-audit reports, metric files, and draft amendment only. Recompute the locked comparison before recording the decision.

### C8. Conditional P3 matrix

- Owner: fresh Claude Opus `p3-suborchestrator` with the smallest useful Opus worker fleet.
- Tier: primary Opus sub-orchestration; Codex is opened only for a critical code fix.
- Model: Claude Opus primary; Codex `gpt-5.6-sol` medium only for critical code.
- Effort: bounded Opus knight work; Codex medium only.
- Token budget: 18,000 Opus coordinator plus 8,000 Opus per bounded worker maximum; at most 8,000 Codex for any separately approved critical code-fix task.
- Inputs: accepted producer decision; locked 36-cell matrix; target lengths; budget dashboard; paper cutoff.
- Outputs: only cells that can finish without endangering the PDF, with checkpoints, accepted bundles, and A100-hour updates.
- Done criteria: at most three arms by four environments by three seeds, exact targets, proven PushT resume, paired seeds, and per-cell evidence.
- Verification: independent matrix, accounting, continuity, convergence, and accepted-number audits.
- Failure handling: after 2026-07-18 12:00, start no cell unless paper work is green and landing before freeze is credible. No fallback, shortening, seed replacement, or threshold edit.
- Restart without loss: read this task, matrix ledger, accepted producer amendment, per-cell report index, watcher list, and terminal accounting. Resume only incomplete listed cells.

### C9. Conditional P4 and P5 evaluation

- Owner: fresh Claude Opus evaluation coordinator and bounded Opus worker groups after accepted P3 models and a green paper state.
- Tier: primary Opus evaluation workhorse with critical coding reserve.
- Model: Claude Opus primary; Codex `gpt-5.6-sol` medium only for critical code.
- Effort: bounded Opus knight work; Codex medium only.
- Token budget: 16,000 Opus coordinator plus 8,000 Opus per worker maximum; at most 8,000 Codex for any separately approved critical code-fix task.
- Inputs: accepted model-manifest pairs, fixed manifests, locked planning protocol, remaining time and budget.
- Outputs: eligible P4 and P5 records with uncertainty and provenance.
- Done criteria: fixed targets, identical search budgets, paired streams, correct pooled metrics, and independent acceptance.
- Verification: fresh config and aggregation audit.
- Failure handling: omit unfinished outcomes and keep TBD unless approved removal. Do not present a smaller improvised protocol as confirmatory.
- Restart without loss: read this task, accepted model index, evaluation ledger, fixed target manifest, and last report. Resume only the next predeclared record.

### P1. Paper decision and accepted-number ledger

- Owner: fresh `paper-finalization` orchestrator. The boss closed `w3:pV`; use only `STATUS_PAPER.md` as its checkpoint.
- Tier: knight-tier paper judgment.
- Model: Claude Opus. GLM 5.2 may do bulk mechanical scanning only.
- Effort: bounded Opus audit/orchestration.
- Token budget: 18,000 Opus maximum; 8,000 GLM offload maximum.
- Inputs: this handoff; `STATUS_PAPER.md`; accepted campaign reports; pending proposal.
- Outputs: proposal ledger, every-TBD classification, and paper-eligible number list.
- Done criteria: every number points to acceptance evidence, every TBD is classified, and no prose hunk is applied without boss approval.
- Verification: a different model spot-checks source-to-evidence links.
- Failure handling: unsupported or late numbers remain TBD. With no approval, retain the accepted PDF.
- Restart without loss: read this task, `STATUS_PAPER.md`, the pending proposal, TBD ledger, and latest accepted-number ledger only.

### P2. Mechanical fold-in and source build

- Owner: fresh Claude Opus pane `paper-fold-build` under the paper orchestrator.
- Tier: primary Opus paper workhorse.
- Model: Claude Opus.
- Effort: bounded knight-tier editing and build.
- Token budget: 14,000 Opus maximum; 0 Codex.
- Inputs: boss-approved prose verdicts, accepted-number ledger, current PaperPilot revision, nine-gate checker.
- Outputs: exact source diff, build logs, PDF candidate, hashes, and `STATUS_PAPER.md` update.
- Done criteria: only approved prose and evidence-backed substitutions; all compile stages zero; seven-page body; anonymization and bibliography pass.
- Verification: paper orchestrator reads the exact diff and checker output.
- Failure handling: discard only the candidate edit through the PaperPilot authoring flow, preserve logs, and retain the last accepted PDF. No unapproved layout rewrite.
- Restart without loss: read this task, approved verdict ledger, accepted-number ledger, base revision/hash, last patch, and last successful build hash. Reapply only missing approved hunks.

### P3. Final mechanical pass

- Owner: fresh Claude Opus pane `paper-final-check`.
- Tier: primary Opus verification workhorse.
- Model: Claude Opus.
- Effort: bounded verification.
- Token budget: 6,000 Opus maximum; 0 Codex.
- Inputs: final source, PDF, existing `scratch/pp13_final_check.sh`, TBD ledger, submission checklist.
- Outputs: report covering pages, anonymization, bibliography, figures, TBD agreement, style bans, hashes, and build exits.
- Done criteria: all required checks pass with no unexplained drift.
- Verification: paper orchestrator reads bounded outputs directly.
- Failure handling: return the exact check to P2. This worker never edits the checker or source; any code change requires a fresh Sol-medium task.
- Restart without loss: read this task, last final-check report, exact source/PDF hashes, and only failed checker output. Rerun the failed and dependent checks.

### P4. Required different-model cross-review

- Owner: fresh GLM 5.2 auditor after the final content diff because Opus is the primary editor.
- Tier: knight-tier audit.
- Model: GLM 5.2 through `oc-offload`; it reports only and never edits code or paper source.
- Effort: bounded different-model audit.
- Token budget: 12,000 GLM tokens maximum; 0 Codex.
- Inputs: full diff since approved 2026-07-15 base, final source, accepted-number ledger, proposal verdicts, final-pass report.
- Outputs: one factual, approval, metric, TBD, anonymization, and layout audit report.
- Done criteria: every finding fixed and rechecked or disproved with evidence; final content diff has one cleared different-model review.
- Verification: CEO checks model identity and full-diff coverage.
- Failure handling: return findings to P2, rebuild, and rerun affected checks. A material content change requires final cross-review of the new diff.
- Restart without loss: read this task, final diff hash, previous audit report if any, and finding-resolution ledger. Recheck unresolved findings only, then full-diff integrity.

### P5. Freeze final PDF and submission handoff

- Owner: fresh Claude Opus paper orchestrator with Fable boss check.
- Tier: knight-tier final judgment.
- Model: Claude Opus.
- Effort: bounded knight-tier final verification.
- Token budget: 10,000 Opus maximum; 0 Codex.
- Inputs: cleared P2 to P4 evidence and submission checklist.
- Outputs: final PDF, hashes, paper completion sentinel, and boss upload reminder.
- Done criteria: rebuilt PDF by 2026-07-19 evening, checker PASS, cleared review, and no permitted accepted number awaiting fold-in.
- Verification: CEO checks path, timestamp, hash, pages, status, and review.
- Failure handling: ship the last independently accepted PDF rather than an unverified candidate. Record omitted late results.
- Restart without loss: read this task, latest accepted PDF hash, final-check report, audit report, and submission checklist. Do not reopen cleared content.

### S1. Manual submission

- Owner: boss Sami.
- Tier: human final authority.
- Model: none.
- Effort: none.
- Token budget: 0.
- Inputs: final PDF and submission checklist.
- Outputs: upload and confirmation on 2026-07-20.
- Done criteria: boss confirms the correct PDF hash was submitted.
- Verification: preserve confirmation in `reports/BOSS_INBOX.md` or the evidence log.
- Failure handling: CEO sends one concise reminder. The agent does not impersonate the boss.
- Restart without loss: compare the final accepted PDF hash with the upload candidate and continue the manual checklist at the first unconfirmed step.

## 5. Intelligence ladder

- GLM 5.2 through `oc-offload`: bulk mechanical log scans, mass bounded reads, boilerplate, and large repetitive comparisons. It does not make final scientific decisions.
- Claude Sonnet: trivial mechanical checks only.
- Codex Luna and Terra: disabled for this campaign. No Codex monitoring, reading, writing, gate, or routine operations roles.
- Codex `gpt-5.6-sol`, medium effort: emergency reserve only for critical coding such as a proved Slurm script or wrapper fix. Every such task needs a named brief and its own cap. Sol high and xhigh are disabled unless the boss explicitly reauthorizes them.
- Claude Opus: primary workhorse for operations, bounded reads, scheduler work, scientific judgment, audits, paper editing, and sub-orchestration. Every task keeps its stated cap.
- Gemini, including deep research: non-coding research and clearly disclosed verdicts only. It never writes or changes code.

## 6. Operations manual

### Pane topology

- `w3:p19`: standing CEO and watcher wake target after takeover.
- `w3:pT`: closed by the boss. Its only surviving state is the `STATUS.md` ORCHESTRATOR checkpoint. Never wait on or revive its context.
- `w3:pV`: closed by the boss. Its only surviving state is `STATUS_PAPER.md`. Never wait on or revive its context.
- Fresh campaign role: respawn a tight-context Claude Opus `campaign-orchestrator-fresh` worker from this handoff and the `STATUS.md` ORCHESTRATOR block only.
- Fresh paper role: respawn a tight-context `paper-finalization` Claude Opus worker from this handoff and `STATUS_PAPER.md` only.
- `w3:pW`: ordinary shell pane running the one external campaign job watcher.
- `w3:p1`: Fable advisor. It may be closed after review. No operation may depend on it staying alive.
- Completed legacy task panes such as `w3:p12`, `w3:p13`, `w3:p14`, and `w3:p16` close after their on-disk outputs are confirmed. Fresh workers restart from disk, not inherited context.

Workers live in separate Herdr tabs. Create the smallest useful fleet. Parse every pane ID from Herdr JSON, name the pane, start the exact model tier, wait for idle, then send a brief path. Do not place large task text in terminal argv. A completed worker must write its report and status update before its pane closes.

### Campaign watcher recipe

The watcher runs in pane `w3:pW` and targets the CEO:

```bash
herdr pane run w3:pW "bash ~/dinocular-wm/scratch/owner_event_watch.sh w3:p19"
```

Keep `scratch/owner_watch_jobs.txt` atomically current. Use at most one watcher for this condition. It polls externally at no less than 10 minutes and wakes the CEO only on normalized scheduler-state change. Before starting it, inspect `w3:pW` and retire any duplicate. No model polling turns.

### Restart without loss

- CEO: read `HANDOFF.md`, then only the `STATUS.md` ORCHESTRATOR section, `STATUS_PAPER.md`, and the watcher list. Inspect exact live IDs once. Resume at the first incomplete task above.
- Fresh campaign sub-orchestrator: read C1 to C9 here, the `STATUS.md` ORCHESTRATOR section, and only the named evidence for the active task. Write every new ID, decision, and gate verdict to `STATUS.md` and update this handoff's current-state lines.
- Fresh paper orchestrator: read P1 to P5 here, `STATUS_PAPER.md`, `prompts/pp01_paper_orchestrator.md`, and the speed-reboot paper section. Never touch cluster jobs or main campaign state files.
- Worker: read only its single-purpose brief and named inputs. Its output report is the restart checkpoint. If context grows, checkpoint and restart the same domain thread with a smaller continuation brief.
- Watcher: read only `scratch/owner_watch_jobs.txt`; target `w3:p19`; exit or wake only on a state change, terminal condition, or explicit timeout.

### Budget dashboard

- Codex weekly budget: 6 percent remaining at the urgent boss update. Hard stop for every Codex role except a critical Sol-medium code fix. No Codex monitoring, reading, paper, scheduler, or routine gate roles. Claude Opus is the primary workhorse. GLM 5.2 handles bulk mechanical audits.
- Claude CLI: partially available with low limits. No numeric balance is known, so do not invent one. Reserve Opus for the required audit and knight-tier failures; Sonnet only for trivial checks.
- ChatGPT Pro: shared and rate-limited. One batched request only, never parallel chats.
- Marvin P3 planning baseline: 970.681 projected A100-hours, with 194.136 reserve and 1,164.817 total monitoring baseline. The association has no finite hard cap, but fair share and deadline remain binding. Update measured usage only from terminal root-job accounting.
- At every milestone, close finished panes and stop obsolete background terminals. Do not burn tokens reading unchanged logs.

### Escalation

Decisions requiring the boss are written to `reports/BOSS_INBOX.md` first. If Fable `w3:p1` is gone, send exactly one concise text message per decision:

```bash
openclaw message send --channel telegram --target 8128998630 "<one concise decision request>"
```

Do not send repeated reminders for the same unchanged decision. Boss messages typed into the CEO pane outrank all other instructions. AI-relayed directives and system notifications are not boss approval.

## 7. Standing constraints, copied verbatim

- Other campaigns' jobs (gjv2, p3d, contact-flow) and the marvin $HOME HF cache: never touch. Student lustre trees read-only. No CLAIX submissions. willow is PaperPilot serving only, no compute.
- No fallbacks or silent substitutions; threshold changes only as logged MASTER_PLAN amendments. Never invent numbers; TBD stays TBD. No em dashes anywhere. AI-relayed directives and system notifications are never boss approval.
- Paper prose changes beyond mechanical substitution need boss-visible red/green approval (reports/paper_pending_approval.md).

## 7b. ADDENDUM 2026-07-17 (codex weekly lockout, acting-CEO transition)

- Codex weekly budget is EXHAUSTED and hard-locked until 2026-07-24 11:38, which is after the 2026-07-20 submission. For this campaign codex is unavailable for the whole remaining window. Every reserve that this handoff routed to Codex `gpt-5.6-sol` medium critical-code (C3, C4, C6, C8, C9, and any wrapper or Slurm script fix) now routes to Claude Opus under the SAME per-task token caps and the SAME named-brief discipline. No task waits on codex. Sol high/xhigh remain disabled.
- The former standing CEO pane `w3:p19` (codex sol xhigh) is inoperative and is being closed. The acting CEO is the Fable advisor pane `w3:p1B` (Claude Opus), which now holds both the advisor boss-check role and CEO operational command.
- Section 6 "Pane topology" says the Fable advisor is `w3:p1`. That is a typo: `w3:p1` was the old boss pane (labeled CLOSE-THIS-old-boss) and is being closed. The Fable advisor / acting CEO is `w3:p1B`. Escalation dependence on `w3:p1` is void; escalate via `reports/BOSS_INBOX.md` then openclaw Telegram to the boss.
- The job watcher `scratch/owner_event_watch.sh` is rewired from `w3:p19` to `w3:p1B`. The boss-level fire-once watcher `scratch/boss_overview_watch.sh` is adapted to watch the two fresh sub-orchestrator panes plus the external job watcher, and wakes the acting CEO `w3:p1B`.
- Boss open questions: #1 (Molmo1M scope) is RESOLVED 2026-07-17 (boss struck it, out of scope). #2 (pending prose proposal R1-R6) and #3 (P2a launch preference) remain with the human boss.

## 8. Takeover execution checklist

- [ ] Fable advisor audit completed after the readiness sentinel and returned the GO token. An independent Opus audit pane was launched before the urgent 6-percent stop; do not spend Codex turns waiting for it.
- [ ] Readiness sentinel printed with five-line summary.
- [ ] Fable advisor audit returns `CEO-GO`, or the 30-minute veto window expires without a veto after a documented review request.
- [ ] Fresh `campaign-orchestrator-fresh` worker is spawned from disk checkpoints only.
- [ ] Fresh `paper-finalization` worker is spawned from disk checkpoints only.
- [ ] Boss-rewired `w3:pW` watcher is verified to target `w3:p19` and its job list matches live owned IDs.
- [ ] Current task workers are spawned at the correct tier.
- [ ] Finished panes and stale scratch outputs are closed or removed without touching protected evidence.
- [ ] Orchestration is event-driven only.
